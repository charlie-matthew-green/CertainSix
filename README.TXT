To generate test input data for the '/trip/{tripId}/containers' endpoint run the 'GenerateTestInput' project.

The web API project has been deployed as an Api app to Azure along with an Azure SQL database with the same tables and stored procedures as found in the database project in the solution. If the console app fails for any reason, try running it again.

Swagger UI: http://shippingcontainerspoilagewebapi.azurewebsites.net/swagger/ui/index